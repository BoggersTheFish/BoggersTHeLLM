.venv/
venv/
# Hugging Face Hub caches (see --hf-cache-dir; default data/cache/hf)
data/cache/
# Local checkpoint weights (use --checkpoint-dir outside repo or upload artifacts separately)
checkpoints/
experiments/
# Local TSCore data dir used by smoke / dev runs
.tscore_smoke/
# Local training / metric dumps (use --epoch-metrics-csv paths you choose)
metrics.csv
# Root-only: do not ignore docs/runs/ (committed reference artifacts)
/runs/
docs/BASELINE_LAST_RUN.txt
__pycache__/
*.py[cod]
.Python
*.egg-info/
.eggs/
dist/
build/
